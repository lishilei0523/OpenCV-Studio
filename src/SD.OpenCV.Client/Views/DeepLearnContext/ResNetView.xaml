<mah:MetroWindow x:Class="SD.OpenCV.Client.Views.DeepLearnContext.ResNetView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:sd="https://github.com/lishilei0523/SD.Framework"
                 xmlns:cm="http://www.caliburnproject.org"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:busyIndicator="https://github.com/Peoky/BusyIndicator"
                 mc:Ignorable="d"
                 Title="ResNet图像分类"
                 Width="1440" Height="900"
                 FontSize="14"
                 WindowStartupLocation="CenterScreen"
                 Style="{StaticResource MetroWindow.Styles.Clean}">

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>
            <Button Content="重置" cm:Message.Attach="Reset()" />
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <Grid>
        <Grid.RowDefinitions>
            <!--Menu-->
            <RowDefinition Height="Auto" />
            <!--Body-->
            <RowDefinition Height="*" />
            <!--Footer-->
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>
        <!--Menu-->
        <Border Grid.Row="0" Style="{StaticResource Panel.Styles.Function.Border}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Menu Grid.Column="0" Margin="4,2">
                    <MenuItem Header="打开ResNet模型(_R)" cm:Message.Attach="OpenResNetModel()" />
                    <MenuItem Header="打开目标图像(_T)" cm:Message.Attach="OpenTargetImage()" />
                    <MenuItem Header="执行分类(_C)" cm:Message.Attach="ExecuteClassify()" />
                </Menu>
                <WrapPanel Grid.Column="1" Margin="0,0,4,0" VerticalAlignment="Center">
                    <TextBlock Text="分类阈值：" />
                    <mah:NumericUpDown Minimum="0" Maximum="100"
                                       Value="{Binding Threshold, UpdateSourceTrigger=PropertyChanged}"
                                       Style="{StaticResource NumericUpDown.Styles.Default}" />
                </WrapPanel>
            </Grid>
        </Border>
        <!--Body-->
        <busyIndicator:BusyMask Grid.Row="1" IndicatorType="Cupertino" IsBusy="{Binding IsBusy}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="300" />
                </Grid.ColumnDefinitions>
                <GroupBox Grid.Column="0" Header="目标图像" Margin="4,4,2,4" Background="LightGray">
                    <sd:ImageViewer ImageSource="{Binding TargetImage}" />
                </GroupBox>
                <GroupBox Grid.Column="1" Header="分类结果" Margin="2,4,4,4">
                    <ListBox ItemsSource="{Binding Predictions}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="5,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Confidence, StringFormat={}{0:F3}}" />
                                    <TextBlock Grid.Column="1" Text="{Binding Label}" Margin="20,0,0,0" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </GroupBox>
                <GridSplitter Grid.Column="0" Style="{StaticResource GridSplitter.Vertical}" />
            </Grid>
        </busyIndicator:BusyMask>
        <!--Footer-->
        <Border Grid.Row="2" Style="{StaticResource Panel.Styles.Function.Border}" BorderThickness="0,1,0,0">
            <Grid>
                <WrapPanel Margin="10,0,10,0" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="Pixel format: " FontSize="12" />
                    <TextBlock Text="{Binding TargetImage.Format}" FontSize="12" />
                    <TextBlock Text=", " FontSize="12" />
                    <TextBlock Text="Width: " FontSize="12" />
                    <TextBlock Text="{Binding TargetImage.Width}" FontSize="12" />
                    <TextBlock Text=", " FontSize="12" />
                    <TextBlock Text="Height: " FontSize="12" />
                    <TextBlock Text="{Binding TargetImage.Height}" FontSize="12" />
                </WrapPanel>
            </Grid>
        </Border>
    </Grid>
</mah:MetroWindow>
