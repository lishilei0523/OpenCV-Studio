<mah:MetroWindow x:Class="SD.OpenCV.Client.Views.DrawContext.ShapeView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:cm="http://caliburnmicro.com"
                 xmlns:busyIndicator="https://github.com/Peoky/BusyIndicator"
                 xmlns:sd="https://github.com/lishilei0523/SD.Framework"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 mc:Ignorable="d"
                 Title="绘制形状" 
                 Width="1440" Height="900" FontSize="14"
                 WindowStartupLocation="CenterOwner"
                 Style="{StaticResource MetroWindow.Styles.Clean}">

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>
            <Button Content="重置" cm:Message.Attach="Reset()" />
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <Grid>
        <Grid.RowDefinitions>
            <!--Menu & ToolBar-->
            <RowDefinition Height="Auto" />
            <!--Body-->
            <RowDefinition Height="*" />
            <!--控制区-->
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <!--Menu & ToolBar-->
        <Border Grid.Row="0" Style="{StaticResource Panel.Styles.Function.Border}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <!--Menu-->
                <Menu Grid.Column="0" Margin="4,2">
                    <MenuItem Header="绘制形状(_D)" cm:Message.Attach="DrawShapes()" />
                    <MenuItem Header="制作掩膜(_M)" cm:Message.Attach="MakeMask()" />
                    <MenuItem Header="适用掩膜(_A)" cm:Message.Attach="ApplyMask()" />
                    <MenuItem Header="分割图像(_S)" cm:Message.Attach="SegmentImage()" />
                </Menu>
                <!--ToolBar-->
                <WrapPanel Grid.Column="1" Margin="0,0,4,0" VerticalAlignment="Center">
                    <ToggleButton ToolTip="缩放" x:Name="ScaleChecked" IsChecked="True"
                  Style="{StaticResource ToggleButton.Styles.ToolBar}">
                        <iconPacks:PackIconMaterialDesign Kind="ZoomIn" />
                    </ToggleButton>
                    <ToggleButton ToolTip="拖拽" x:Name="DragChecked"
                  Style="{StaticResource ToggleButton.Styles.ToolBar}">
                        <iconPacks:PackIconMaterialDesign Kind="Navigation" />
                    </ToggleButton>
                    <ToggleButton ToolTip="编辑" x:Name="ResizeChecked"
                  Style="{StaticResource ToggleButton.Styles.ToolBar}">
                        <iconPacks:PackIconMaterialDesign Kind="UnfoldMore" />
                    </ToggleButton>
                    <ToggleButton ToolTip="点" x:Name="PointChecked"
                  Style="{StaticResource ToggleButton.Styles.ToolBar}">
                        <iconPacks:PackIconMaterialDesign Kind="BubbleChart" />
                    </ToggleButton>
                    <ToggleButton ToolTip="线段" x:Name="LineChecked"
                  Style="{StaticResource ToggleButton.Styles.ToolBar}">
                        <iconPacks:PackIconMaterialDesign Kind="Remove" />
                    </ToggleButton>
                    <ToggleButton ToolTip="画刷" x:Name="BrushChecked"
                  Style="{StaticResource ToggleButton.Styles.ToolBar}">
                        <iconPacks:PackIconMaterialDesign Kind="Brush" />
                    </ToggleButton>
                    <ToggleButton ToolTip="矩形" x:Name="RectangleChecked"
                  Style="{StaticResource ToggleButton.Styles.ToolBar}">
                        <iconPacks:PackIconMaterialDesign Kind="CropLandscape" />
                    </ToggleButton>
                    <ToggleButton ToolTip="旋转矩形" x:Name="RotatedRectangleChecked"
                  Style="{StaticResource ToggleButton.Styles.ToolBar}">
                        <iconPacks:PackIconMaterialDesign Kind="CropSquare">
                            <iconPacks:PackIconMaterialDesign.RenderTransform>
                                <RotateTransform Angle="45" CenterX="8" CenterY="8" />
                            </iconPacks:PackIconMaterialDesign.RenderTransform>
                        </iconPacks:PackIconMaterialDesign>
                    </ToggleButton>
                    <ToggleButton ToolTip="圆形" x:Name="CircleChecked"
                  Style="{StaticResource ToggleButton.Styles.ToolBar}">
                        <iconPacks:PackIconMaterialDesign Kind="PanoramaFishEye" />
                    </ToggleButton>
                    <ToggleButton ToolTip="椭圆形" x:Name="EllipseChecked"
                  Style="{StaticResource ToggleButton.Styles.ToolBar}">
                        <iconPacks:PackIconMaterialDesign Kind="PanoramaFishEye">
                            <iconPacks:PackIconMaterialDesign.RenderTransform>
                                <ScaleTransform ScaleX="1.3" CenterX="8" />
                            </iconPacks:PackIconMaterialDesign.RenderTransform>
                        </iconPacks:PackIconMaterialDesign>
                    </ToggleButton>
                    <ToggleButton ToolTip="多边形" x:Name="PolygonChecked"
                  Style="{StaticResource ToggleButton.Styles.ToolBar}">
                        <iconPacks:PackIconMaterialDesign Kind="StarBorder" />
                    </ToggleButton>
                    <ToggleButton ToolTip="折线段" x:Name="PolylineChecked"
                  Style="{StaticResource ToggleButton.Styles.ToolBar}">
                        <iconPacks:PackIconMaterialDesign Kind="Timeline" />
                    </ToggleButton>
                </WrapPanel>
            </Grid>
        </Border>
        <!--Body-->
        <busyIndicator:BusyMask Grid.Row="1" IndicatorType="Cupertino" IsBusy="{Binding IsBusy}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="275" />
                </Grid.ColumnDefinitions>
                <!--图像画布-->
                <GroupBox Grid.Column="0" Margin="4,4,2,4" Background="LightGray">
                    <!--Header-->
                    <GroupBox.HeaderTemplate>
                        <DataTemplate>
                            <Grid DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=Window},Path=DataContext}">
                                <TextBlock Text="图像画布" />
                                <WrapPanel Margin="0,0,4,0" HorizontalAlignment="Right">
                                    <TextBlock Text="Mouse position: " FontSize="12" />
                                    <TextBlock Text="(" FontSize="12" />
                                    <TextBlock Text="{Binding MousePositionX}" FontSize="12" />
                                    <TextBlock Text=", " FontSize="12" />
                                    <TextBlock Text="{Binding MousePositionY}" FontSize="12" />
                                    <TextBlock Text=")" FontSize="12" />
                                    <TextBlock Text=" | " FontSize="12" />
                                    <TextBlock Text="Pixel format: " FontSize="12" />
                                    <TextBlock Text="{Binding BitmapSource.Format}" FontSize="12" />
                                    <TextBlock Text=" | " FontSize="12" />
                                    <TextBlock Text="Width: " FontSize="12" />
                                    <TextBlock Text="{Binding BitmapSource.Width}" FontSize="12" />
                                    <TextBlock Text=" | " FontSize="12" />
                                    <TextBlock Text="Height: " FontSize="12" />
                                    <TextBlock Text="{Binding BitmapSource.Height}" FontSize="12" />
                                </WrapPanel>
                            </Grid>
                        </DataTemplate>
                    </GroupBox.HeaderTemplate>
                    <sd:DrawableCanvasEx BorderBrush="{Binding BorderBrush}"
                                         BorderThickness="{Binding BorderThickness}"
                                         GuideLinesVisibility="{Binding GuideLinesVisibility}"
                                         ScaleChecked="{Binding ElementName=ScaleChecked, Path=IsChecked, Mode=TwoWay}"
                                         DragChecked="{Binding ElementName=DragChecked, Path=IsChecked, Mode=TwoWay}"
                                         ResizeChecked="{Binding ElementName=ResizeChecked, Path=IsChecked, Mode=TwoWay}"
                                         PointChecked="{Binding ElementName=PointChecked, Path=IsChecked, Mode=TwoWay}"
                                         LineChecked="{Binding ElementName=LineChecked, Path=IsChecked, Mode=TwoWay}"
                                         BrushChecked="{Binding ElementName=BrushChecked, Path=IsChecked, Mode=TwoWay}"
                                         RectangleChecked="{Binding ElementName=RectangleChecked, Path=IsChecked, Mode=TwoWay}"
                                         RotatedRectangleChecked="{Binding ElementName=RotatedRectangleChecked, Path=IsChecked, Mode=TwoWay}"
                                         CircleChecked="{Binding ElementName=CircleChecked, Path=IsChecked, Mode=TwoWay}"
                                         EllipseChecked="{Binding ElementName=EllipseChecked, Path=IsChecked, Mode=TwoWay}"
                                         PolygonChecked="{Binding ElementName=PolygonChecked, Path=IsChecked, Mode=TwoWay}"
                                         PolylineChecked="{Binding ElementName=PolylineChecked, Path=IsChecked, Mode=TwoWay}"
                                         ItemsSource="{Binding Shapes}"
                                         DatasSource="{Binding ShapeLs}"
                                         cm:Message.Attach="
                                         [Event ShapeClick] = [Action OnShapeMouseLeftDown($source,$eventArgs)];
                                         [Event MouseMove] = [Action OnCanvasMouseMove($source)];">
                        <sd:CanvasEx.BackgroundImage>
                            <Image Source="{Binding BitmapSource}" />
                        </sd:CanvasEx.BackgroundImage>
                    </sd:DrawableCanvasEx>
                </GroupBox>
                <!--参数&结果-->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="210"/>
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <!--绘制设置-->
                    <GroupBox Grid.Row="0" Header="绘制参数" Margin="2,4,4,2">
                        <StackPanel Grid.IsSharedSizeScope="True" Margin="5,20">
                            <sd:FormItem Label="边框颜色：">
                                <mah:ColorPicker SelectedColor="{Binding BorderColor}" Style="{StaticResource ColorPicker.Styles.Fluent}" />
                            </sd:FormItem>
                            <sd:FormItem Label="边框粗细：">
                                <mah:NumericUpDown Minimum="1" Maximum="100"
                                                   Value="{Binding BorderThickness, UpdateSourceTrigger=PropertyChanged}"
                                                   Style="{StaticResource NumericUpDown.Styles.Fluent}" />
                            </sd:FormItem>
                            <sd:FormItem Label="参考线：">
                                <mah:ToggleSwitch IsOn="{Binding ShowGuideLines}" OnContent="显示" OffContent="隐藏" 
                                                  Style="{StaticResource ToggleSwitch.Styles.Fluent}"
                                                  cm:Message.Attach="[Event Toggled] = [Action SwitchGuideLines()]" />
                            </sd:FormItem>
                        </StackPanel>
                    </GroupBox>
                    <!--形状列表-->
                    <GroupBox Grid.Row="1" Header="形状列表" Margin="2,2,4,4">
                        <ListBox ItemsSource="{Binding ShapeLs}" SelectedItem="{Binding SelectedShapeL}"
                                 cm:Message.Attach="[Event SelectionChanged] = [Action OnSelectShape()]">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Text}" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="复制(_C)" cm:Message.Attach="CopyShape()" />
                                    <MenuItem Header="删除(_D)" cm:Message.Attach="RemoveShape()" />
                                </ContextMenu>
                            </ListBox.ContextMenu>
                        </ListBox>
                    </GroupBox>
                </Grid>
                <GridSplitter Grid.Column="0" Style="{StaticResource GridSplitter.Vertical}" />
            </Grid>
        </busyIndicator:BusyMask>
        <!--控制区-->
        <Grid Grid.Row="2" Style="{StaticResource Form.Styles.Submit.Grid}">
            <WrapPanel Style="{StaticResource Form.Styles.Submit.WrapPanel}">
                <Button Content="确定" cm:Message.Attach="TryCloseAsync('true')" Style="{StaticResource Button.Styles.Fluent.Focus}" />
                <Button Content="取消" cm:Message.Attach="TryCloseAsync('false')" Style="{StaticResource Button.Styles.Fluent.Default}" />
            </WrapPanel>
        </Grid>
    </Grid>
</mah:MetroWindow>
